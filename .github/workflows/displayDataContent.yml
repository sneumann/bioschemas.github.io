name: display_data_folder_content
on: [push]
jobs:
  generate-profile:
    runs-on: ubuntu-latest

    steps:       
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Checkout submodules
        uses: textbook/git-checkout-submodule-action@v3
      - name: ls
        run: ls specifications